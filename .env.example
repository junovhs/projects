# Vercel Blob Storage total byte limit (optional)
# BLOB_TOTAL_BYTES=100000000

# Password for bearer token authentication on protected API endpoints
AI_API_PASSWORD=

# Allow-list of hostnames for the asset-sucker proxy (comma-separated)
PROXY_ALLOW_HOSTS=images.unsplash.com,your-cdn.com

# Max bytes for the asset-sucker proxy
PROXY_MAX_BYTES=10000000

# CORS origin for the proxy (if needed, otherwise defaults to same-origin)
# PROXY_CORS_ORIGIN=https://your-frontend-domain.com